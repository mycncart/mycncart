<div class="table-responsive">
  <table class="table table-bordered">
    <thead>
      <tr>
        <td class="text-left" colspan="2"><h5>商品规格</h5></td>
      </tr>
    </thead>
    <tbody>
      {% if options %}
        {% for option in options %}
          <tr>
            <td class="text-left">{{ option.name }}</td>
            <td class="text-left">
				{% for option_value in option.option_values %}
				
				<button type="button" id="button_option_value_id_{{ option_value.option_value_id }}" data-option_id="{{ option.option_id }}" data-option_value_id="{{ option_value.option_value_id }}" class="btn btn-default" onclick="change_value({{ option_value.option_value_id }})">{{ option_value.name }}</button>
				
					{% if option_value.image %}
					<img src="{{ option_value.image }}" onclick="GetUploadify3('1');" width="35" height="35">
					{% endif %}
					
				<input type="hidden" name="item_img[1]" value="">
				
				{% endfor %}
			</td>
          </tr>
        {% endfor %}
      {% else %}
        <tr>
          <td class="text-center">{{ text_no_results }}</td>
        </tr>
      {% endif %}
    </tbody>
  </table>
</div>
